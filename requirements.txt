# Core dependencies
# No core dependencies required

# Optional GUI dependencies
PySide6>=6.0.0; platform_system != "Linux"  # For Windows and macOS
PySide6>=6.0.0; platform_system == "Linux"  # For Linux

# macOS: Cocoa API for wallpaper scaling (pre-installed with system Python;
# install manually if using a non-system Python: pip install pyobjc-framework-Cocoa)
pyobjc-framework-Cocoa>=9.0; platform_system == "Darwin"